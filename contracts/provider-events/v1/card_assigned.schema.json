{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:wurzburg:provider-events:v1:card_assigned",
  "allOf": [
    { "$ref": "urn:wurzburg:provider-events:v1:envelope" },
    { "properties": { "event_type": { "const": "CARD_ASSIGNED" }, "data": { "type": "object", "additionalProperties": false, "required": ["assignment_type"], "properties": { "assignment_type": { "enum": ["NEW", "EXISTING_MULTI_PROVIDER"] } } } } }
  ]
}
